import { Card, PageHeader } from "@/components/ui/DashboardUI";
import { courses } from "@/data/dashboardMockData";
export default function StudentProgressPage() { return <div className="space-y-7"><PageHeader eyebrow="Learning analytics" title="My Progress" description="See how your learning rhythm is building over time." /><div className="grid gap-5 md:grid-cols-2">{courses.slice(0, 3).map((course) => <Card key={course.id} className="p-5"><div className="flex justify-between gap-4"><div><h2 className="text-sm font-semibold text-white">{course.title}</h2><p className="mt-1 text-xs text-slate-500">{course.instructor}</p></div><span className="text-sm font-bold text-orange-300">{course.progress}%</span></div><div className="mt-5 h-2 rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-orange-500 to-amber-400" style={{ width: `${course.progress}%` }} /></div></Card>)}</div></div>; }
