"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import Icon from "@/components/dashboard/Icon";
import { Card, EmptyState } from "@/components/ui/DashboardUI";
import { getBlogImageUrl, getPublishedBlogs, blocksToText } from "@/services/strapi/blogs";
export default function PublicBlogList() { const [blogs, setBlogs] = useState(null); useEffect(() => { getPublishedBlogs().then((response) => setBlogs(response?.data || [])).catch((error) => { console.error("[blogs] Public load failed", error); setBlogs([]); }); }, []); if (!blogs) return <div className="mx-auto max-w-6xl animate-pulse p-10"><div className="h-10 w-1/3 rounded bg-slate-200" /><div className="mt-8 h-80 rounded-2xl bg-slate-200" /></div>; return <div className="mx-auto max-w-6xl space-y-8 px-4 py-10 sm:px-6 lg:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-orange-500">LearnHub Journal</p><h1 className="mt-2 text-4xl font-bold text-slate-900">Ideas for building better software</h1><p className="mt-3 text-slate-600">Practical perspectives from the learning community.</p></div>{blogs.length ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{blogs.map((blog) => <Card key={blog.documentId || blog.id} className="overflow-hidden bg-white"><div className="h-48 bg-slate-100">{getBlogImageUrl(blog.image) ? <img src={getBlogImageUrl(blog.image)} alt="" className="h-full w-full object-cover" /> : <div className="flex h-full items-center justify-center text-orange-500"><Icon name="edit" size={40} /></div>}</div><div className="p-5"><p className="text-xs text-slate-500">{new Date(blog.publishedAt).toLocaleDateString()}</p><h2 className="mt-2 line-clamp-2 text-lg font-bold text-slate-900">{blog.title}</h2><p className="mt-2 line-clamp-3 text-sm leading-6 text-slate-600">{blocksToText(blog.Description)}</p><p className="mt-3 text-xs text-slate-500">By {blog.creator?.username || "LearnHub author"}</p><Link href={`/blog/${blog.documentId || blog.id}`} className="mt-4 inline-flex text-sm font-semibold text-orange-600">Read article →</Link></div></Card>)}</div> : <EmptyState title="No published blogs yet" description="Check back soon for new learning articles." />}</div>; }