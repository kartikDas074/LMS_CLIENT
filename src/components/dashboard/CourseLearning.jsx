"use client";

import { useState } from "react";
import Link from "next/link";
import Icon from "@/components/dashboard/Icon";
import { Card, PageHeader } from "@/components/ui/DashboardUI";
import { courses, lessonsForCourse } from "@/data/dashboardMockData";

export default function CourseLearning({ courseId }) {
  const course = courses.find((item) => item.id === courseId) || courses[0];
  const [currentLesson, setCurrentLesson] = useState(2);
  const lesson = lessonsForCourse[currentLesson];
  return <div className="space-y-7"><Link href="/dashboard/student/courses" className="inline-flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-orange-300"><span className="rotate-180"><Icon name="arrow" size={14} /></span> Back to my courses</Link><PageHeader eyebrow="Learning environment" title={course.title} description={course.description} /><div className="grid gap-6 xl:grid-cols-[280px_minmax(0,1fr)]"><Card className="h-fit overflow-hidden"><div className="border-b border-slate-800 px-4 py-4"><div className="flex justify-between text-xs"><span className="font-semibold text-slate-200">Course progress</span><span className="font-bold text-orange-300">{course.progress}%</span></div><div className="mt-3 h-1.5 rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-orange-500 to-amber-400" style={{ width: `${course.progress}%` }} /></div></div><div className="space-y-1 p-3">{lessonsForCourse.map((item, index) => <button type="button" key={item.title} onClick={() => setCurrentLesson(index)} className={`flex w-full items-start gap-3 rounded-xl px-2.5 py-3 text-left transition ${currentLesson === index ? "bg-orange-500/10" : "hover:bg-slate-800/50"}`}><span className={`mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full border text-[10px] ${item.completed ? "border-emerald-400/40 bg-emerald-400/10 text-emerald-300" : currentLesson === index ? "border-orange-400 bg-orange-500 text-white" : "border-slate-700 text-slate-600"}`}>{item.completed ? <Icon name="check" size={12} /> : index + 1}</span><span className="min-w-0"><span className={`block text-xs font-medium ${currentLesson === index ? "text-orange-200" : "text-slate-300"}`}>{item.title}</span><span className="mt-1 block text-[10px] text-slate-600">{item.duration}</span></span></button>)}</div></Card><div className="min-w-0 space-y-5"><Card className="overflow-hidden"><div className="flex aspect-video items-center justify-center bg-gradient-to-br from-slate-900 via-slate-950 to-orange-950/30"><div className="text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-orange-500 text-white shadow-lg shadow-orange-500/25"><Icon name="play" size={22} /></div><p className="mt-4 text-xs font-medium text-slate-400">Video lesson placeholder</p></div></div><div className="p-5 sm:p-7"><p className="text-[11px] font-bold uppercase tracking-[0.2em] text-orange-400">Lesson {currentLesson + 1}</p><h2 className="mt-2 text-xl font-bold text-white">{lesson.title}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Work through this lesson at your own pace. Video, notes, resources, and interactive exercises will be connected to Strapi later.</p><div className="mt-6 flex flex-col gap-3 border-t border-slate-800 pt-5 sm:flex-row sm:items-center sm:justify-between"><button type="button" className="inline-flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-orange-500 to-amber-400 px-4 py-2.5 text-xs font-semibold text-white shadow-lg shadow-orange-500/10 hover:brightness-110"><Icon name="check" size={14} /> Mark complete</button><button type="button" onClick={() => setCurrentLesson(Math.min(currentLesson + 1, lessonsForCourse.length - 1))} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-700 px-4 py-2.5 text-xs font-semibold text-slate-300 hover:border-orange-500/50 hover:text-orange-300">Next lesson <Icon name="arrow" size={14} /></button></div></div></Card></div></div></div>;
}
